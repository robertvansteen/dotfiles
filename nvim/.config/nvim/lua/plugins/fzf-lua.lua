return {
  {
    "ibhagwan/fzf-lua",
    opts = {
      defaults = {
        hidden = true,
      },
      grep = {
        rg_opts = "--column --line-number --no-heading --color=always --smart-case --hidden",
      },
    },
  },
}
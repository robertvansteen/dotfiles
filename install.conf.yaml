- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vimrc: vimrc
    ~/.shell: shell
    ~/.zshrc: zshrc
    ~/.ideavimrc: ideavimrc

- shell:
  - [git submodule update --init --recursive, Installing submodules...]
  - [brew bundle]
